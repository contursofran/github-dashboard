import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => ({
  content: {
    alignSelf: "stretch",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  grid: {
    alignSelf: "start",
  },
}));

export { useStyles };
