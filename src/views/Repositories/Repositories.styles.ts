import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => ({
  content: {
    display: "flex",
    alignSelf: "center",
    alignItems: "center",
    justifyContent: "center",
  },

  grid: {
    paddingBottom: "15px",
  },
}));

export { useStyles };
